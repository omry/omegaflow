---
sidebar_position: 2
sidebar_label: Quickstart
---

# Quickstart

Start with the recording generated by bootstrap. It is intentionally small: one
Markdown file with two short terminal sections named **First Video Beat** and
**Second Video Beat**. Their simple content makes it easy to see how beats
appear as previewable sections in the generated player.

```bash
omegaflow bootstrap=project
omegaflow recording=test-video
```

The `test-video` recording is not checked into this repository as source. It is
created by `bootstrap=project` in the current recording workspace.
