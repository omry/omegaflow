Synchronize captured terminal and browser pane actions in both directions
through fully qualified start and end events. Preserve observed action pacing
when joined actions are relocated into presentation time, and keep each
persistent capture runner on its owning thread through setup, capture, cleanup,
and close.
