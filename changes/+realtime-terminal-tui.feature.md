Restore `timing: realtime` as the live terminal-capture control, attach realtime
commands to a pseudo-terminal, render common TUI updates in place, and support
readiness-aware text, named-key, control-key, and pause input steps.
