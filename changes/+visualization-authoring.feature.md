Add typed multi-pane authoring that builds a syntax-colored visualization
beside captured terminal or browser content. Frame and label each pane so
composed layouts remain visually distinct, with configurable pane titles,
edge-relative title placement, and an undecorated pane-chrome style. Declare
global pane streams once in the Markdown body before their beats, outside
overrideable recording configuration. Allow explicit terminal pane actions to
preserve realtime output timing. Support sequential visualization pane beats
whose start times join fully qualified narration segment events, and let
container-beat highlight effects target terminal or visualization recording
surfaces without discarding syntax coloring.
