Track optional command-level recording dependencies. Declared inputs now
control capture freshness and watch rebuilds. Named produced outputs validate
producer results and coordinate dependent terminal commands across panes. Use
`force=true` to bypass freshness reuse.
