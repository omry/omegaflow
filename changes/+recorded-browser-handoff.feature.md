Allow the recorder to take over the managed browser opened by a blocking
`action=watch` terminal command, so one workflow can authentically transition
from CLI output into a scripted browser beat without launching an unmanaged
browser.
