Implement the amended OmegaFlow Envoy protocol v1 contracts in Go: bounded
controller and workload telemetry with output marks, gate interruption, and
typed workload inspection, the private external-awsh frame protocol and
submission-capsule bounds, path resolution and directory digests, and a
canonical golden-fixture conformance corpus with fail-closed lifecycle
validation.
