Package a manifest-locked Linux Envoy and Awsh workload runtime, add typed
Hydra composition for Reploy controller and workload blueprints, and stage the
installed runtime as a fresh read-only `/omegaflow-runtime` bind source.
