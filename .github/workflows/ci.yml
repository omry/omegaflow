name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  envoy-runtime:
    name: Envoy runtime
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache-dependency-path: runtime/envoy/go.mod

      - name: Test workload Envoy with race detector
        working-directory: runtime/envoy
        run: go test -race ./...

      - name: Vet workload Envoy
        working-directory: runtime/envoy
        run: go vet ./...

      - name: Build workload Envoy for Linux amd64
        working-directory: runtime/envoy
        run: >-
          env CGO_ENABLED=0 GOOS=linux GOARCH=amd64
          go build -trimpath -buildvcs=false -ldflags='-s -w -buildid='
          -o /tmp/omegaflow-envoy-amd64 ./cmd/omegaflow-envoy

      - name: Build workload Envoy for Linux arm64
        working-directory: runtime/envoy
        run: >-
          env CGO_ENABLED=0 GOOS=linux GOARCH=arm64
          go build -trimpath -buildvcs=false -ldflags='-s -w -buildid='
          -o /tmp/omegaflow-envoy-arm64 ./cmd/omegaflow-envoy

  python:
    name: Python tests
    runs-on: ubuntu-latest
    env:
      ASCIINEMA_VERSION: v3.2.1
      ASCIINEMA_SHA256: 1b405bbda565b33c3c4718de67fedc3535580603c0694b1ff3fb04f363430a20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and test dependencies
        run: python -m pip install -e ".[dev,browser]"

      - name: Install terminal recorder for retained-host compatibility tests
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL \
            "https://github.com/asciinema/asciinema/releases/download/${ASCIINEMA_VERSION}/asciinema-x86_64-unknown-linux-gnu" \
            -o "$HOME/.local/bin/asciinema"
          echo "${ASCIINEMA_SHA256}  $HOME/.local/bin/asciinema" | sha256sum -c -
          chmod +x "$HOME/.local/bin/asciinema"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          "$HOME/.local/bin/asciinema" --version

      - name: Install Chromium and browser system dependencies
        run: python -m playwright install --with-deps chromium

      - name: Install browser media tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes ffmpeg

      - name: Run canonical CI checks
        run: nox -s ci

      - name: Summary
        if: always()
        run: |
          {
            echo "## Python tests"
            echo ""
            echo "- Installed the package with development dependencies."
            echo "- Installed the pinned terminal recorder only for retained-host compatibility tests."
            echo "- Installed Chromium, FFmpeg, and FFprobe for capture tests."
            echo "- Ran \`nox -s ci\` (tests and generated schema docs)."
            echo "- Job status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"

  package:
    name: Package build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache-dependency-path: runtime/envoy/go.mod

      - name: Install build tooling
        run: python -m pip install build hatchling nox

      - name: Build distributions
        run: nox -s package

      - name: Smoke install Linux wheel
        run: |
          python -m pip install dist/*manylinux_2_35_x86_64.whl
          python -m pip check
          omegaflow --help

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: ci-python-package-distributions
          path: dist/

      - name: Summary
        if: always()
        run: |
          {
            echo "## Package build"
            echo ""
            echo "- Built source and wheel distributions."
            echo "- Smoke-installed the generated wheel."
            echo "- Uploaded distributions as CI artifacts."
            echo "- Job status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"

  integration:
    name: Recording integration
    runs-on: ubuntu-latest
    env:
      ASCIINEMA_VERSION: v3.2.1
      ASCIINEMA_SHA256: 1b405bbda565b33c3c4718de67fedc3535580603c0694b1ff3fb04f363430a20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package
        run: python -m pip install -e .

      - name: Install asciinema 3.x
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL \
            "https://github.com/asciinema/asciinema/releases/download/${ASCIINEMA_VERSION}/asciinema-x86_64-unknown-linux-gnu" \
            -o "$HOME/.local/bin/asciinema"
          echo "${ASCIINEMA_SHA256}  $HOME/.local/bin/asciinema" | sha256sum -c -
          chmod +x "$HOME/.local/bin/asciinema"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          "$HOME/.local/bin/asciinema" --version

      - name: Build generated test recording
        run: |
          project="$RUNNER_TEMP/omegaflow-integration"
          mkdir -p "$project"
          cd "$project"
          omegaflow bootstrap=project
          omegaflow recording=test-video action=build force=true
          test -s recordings/.omegaflow/videos/test-video/presentation/recording.presentation.json
          test -s recordings/.omegaflow/videos/test-video/presentation/recording.recording.json
          test -s recordings/.omegaflow/videos/test-video/presentation/beats/first-video-beat.cast
          test -s recordings/.omegaflow/videos/test-video/presentation/beats/second-video-beat.cast
          test -s recordings/.omegaflow/videos/test-video/index.html

      - name: Upload recording artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-recording-integration-artifacts
          path: ${{ runner.temp }}/omegaflow-integration/recordings/.omegaflow/
          if-no-files-found: ignore
          include-hidden-files: true

      - name: Summary
        if: always()
        run: |
          {
            echo "## Recording integration"
            echo ""
            echo "- Installed asciinema ${ASCIINEMA_VERSION} from the upstream release binary."
            echo "- Bootstrapped a clean OmegaFlow project."
            echo "- Built the generated quickstart recording."
            echo "- Job status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"

  docs:
    name: Website docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: pnpm/action-setup@v4
        with:
          package_json_file: website/package.json

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: website/pnpm-lock.yaml

      - name: Install website dependencies
        run: pnpm --dir website install --frozen-lockfile

      - name: Check generated schema docs
        run: python website/scripts/update_recording_schema_docs.py --check

      - name: Build website
        run: pnpm --dir website build

      - name: Summary
        if: always()
        run: |
          {
            echo "## Website docs"
            echo ""
            echo "- Installed website dependencies with pnpm."
            echo "- Checked generated schema docs."
            echo "- Built the Docusaurus site."
            echo "- Job status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"
